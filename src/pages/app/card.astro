---
import Layout from '@/layouts/Layout.astro';
import RootProvider from '@/lib/providers/RootProvider';
import { ErrorBoundary } from '@/components/ErrorBoundary';
import CardPage from '@/components/app/CardPage';
---

<Layout title="Prime - Digital Card">
  <RootProvider client:only="react">
    <ErrorBoundary client:only="react">
      <CardPage client:only="react" />    </ErrorBoundary>
  </RootProvider>
</Layout>
